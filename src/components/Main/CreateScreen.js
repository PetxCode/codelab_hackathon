import React from "react";
import styled from "styled-components";

export const CreateScreen = () => {
  return (
    <Container>
      <Wrapper>Create Screen</Wrapper>
    </Container>
  );
};

const Wrapper = styled.div``;
const Container = styled.div`
  padding-top: 100px;
`;
