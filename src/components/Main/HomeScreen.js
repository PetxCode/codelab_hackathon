import React from "react";
import styled from "styled-components";

export const HomeScreen = () => {
  return (
    <Container>
      <Wrapper>Home Screen</Wrapper>
    </Container>
  );
};

const Wrapper = styled.div``;
const Container = styled.div`
  padding-top: 100px;
`;
